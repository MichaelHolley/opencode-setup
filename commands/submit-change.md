---
description: Submit a change to the codebase
agent: build
---

STEPS:

1. make the new commit with the command: `git commit -am "<brief-commit-message>"`
2. submit the changes with the command: `git push`
